SELECT ACCOUNT, NVL(SUM(ENDBAL_DEBIT + ENDBAL_CREDIT), 0) AS AMT
            FROM MKBD.MKBD_BALANCE_SUMMARY
            WHERE GEN_DATE = TO_DATE(:genDate, 'yyyy-mm-dd')
              AND ACCOUNT IN ('10501991', '10503011', '10503021', '10503991')
GROUP BY ACCOUNT
ORDER BY ACCOUNT